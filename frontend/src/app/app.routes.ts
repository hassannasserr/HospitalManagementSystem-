import { Routes } from '@angular/router';
import { DoctorLoginComponent } from '../component/doctor-login/doctor-login';

export const routes: Routes = [{ path: 'doctor/login', component: DoctorLoginComponent }];
